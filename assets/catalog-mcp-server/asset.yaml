apiVersion: asset.sap/v1
kind: Asset

metadata:
  name: catalog-mcp-server
  version: 1.0.0
  description: >
    MCP server for SAP Datasphere Catalog Service API (OData).
    Provides tools for discovering and browsing Datasphere spaces and assets.

type: mcp-server

spec:
  translationFile: translation.json
