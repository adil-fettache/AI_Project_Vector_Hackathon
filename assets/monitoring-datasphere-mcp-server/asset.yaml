apiVersion: asset.sap/v1
kind: Asset

metadata:
  name: monitoring-datasphere-mcp-server
  version: "1.0.0"
  description: "MCP server for SAP Datasphere Monitoring Query API — query task run history, health metrics and performance data"

type: mcp-server

destinations:
  - name: DATASPHERE
    description: "SAP Datasphere / BDC tenant REST API base URL"

mcp:
  translationFile: mcp-translation/translation.json
