apiVersion: asset.sap/v1
kind: Asset

metadata:
  name: replication-mcp-server
  version: 1.0.0
  description: >
    MCP server for SAP Datasphere Replication API.
    Provides tools for replicating master data and transactional data from SAP sources.

type: mcp-server

spec:
  translationFile: translation.json
