apiVersion: asset.sap/v1
kind: Asset

metadata:
  name: bdc-cockpit-mcp-server
  version: "1.0.0"
  description: "MCP server for BDC Cockpit APIs — register, publish, and manage the full lifecycle of Data Products in SAP Business Data Cloud"

type: mcp-server

destinations:
  - name: BDC_COCKPIT
    description: "BDC Cockpit REST API base URL (e.g. https://<bdc-tenant>.bdc.cloud.sap)"

mcp:
  translationFile: mcp-translation/translation.json
