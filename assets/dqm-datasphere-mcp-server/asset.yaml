apiVersion: asset.sap/v1
kind: Asset

metadata:
  name: dqm-datasphere-mcp-server
  version: "1.0.0"
  description: "MCP server for SAP Data Quality Management Data Profiling API — run profiling jobs, define quality rules and enforce quality gates"

type: mcp-server

destinations:
  - name: DATASPHERE
    description: "SAP Datasphere / BDC tenant REST API base URL"

mcp:
  translationFile: mcp-translation/translation.json
