apiVersion: asset.sap/v1
kind: Asset

metadata:
  name: enterprise-information-management-mcp-server
  version: 1.0.0
  description: >
    MCP server for SAP Datasphere Enterprise Information Management API.
    Provides tools for managing EIM tasks, executions, and virtual tables.

type: mcp-server

spec:
  translationFile: translation.json
