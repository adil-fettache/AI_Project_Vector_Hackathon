apiVersion: asset.sap/v1
kind: Asset

metadata:
  name: metadata-management-mcp-server
  version: "1.0.0"
  description: "MCP server for SAP Datasphere Metadata Management API — read/write business metadata, lineage, glossary terms and governance attributes"

type: mcp-server

destinations:
  - name: DATASPHERE
    description: "SAP Datasphere / BDC tenant REST API base URL"

mcp:
  translationFile: mcp-translation/translation.json
