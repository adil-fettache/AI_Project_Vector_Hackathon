apiVersion: asset.sap/v1
kind: Asset

metadata:
  name: sac-datasphere-mcp-server
  version: "1.0.0"
  description: "MCP server for SAP Analytics Cloud Integration API — manage connections, models, stories and data imports from Datasphere to SAC"

type: mcp-server

destinations:
  - name: DATASPHERE
    description: "SAP Datasphere / BDC tenant REST API base URL"

mcp:
  translationFile: mcp-translation/translation.json
