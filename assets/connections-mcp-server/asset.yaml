apiVersion: asset.sap/v1
kind: Asset

metadata:
  name: connections-mcp-server
  version: 1.0.0
  description: >
    MCP server for SAP Datasphere Connections API.
    Provides tools for managing SAP Datasphere space connections.

type: mcp-server

spec:
  translationFile: translation.json
