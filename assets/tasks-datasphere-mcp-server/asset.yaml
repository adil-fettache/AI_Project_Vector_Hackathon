apiVersion: asset.sap/v1
kind: Asset

metadata:
  name: tasks-datasphere-mcp-server
  version: "1.0.0"
  description: "MCP server for SAP Datasphere Tasks API — manage and execute task chains, data flows and scheduled jobs"

type: mcp-server

destinations:
  - name: DATASPHERE
    description: "SAP Datasphere / BDC tenant REST API base URL"

mcp:
  translationFile: mcp-translation/translation.json
