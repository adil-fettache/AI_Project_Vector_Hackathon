apiVersion: asset.sap/v1
kind: Asset

metadata:
  name: catalog-datasphere-mcp-server
  version: "1.0.0"
  description: "MCP server for SAP Datasphere Catalog API — discover spaces, objects and data products"

type: mcp-server

destinations:
  - name: DATASPHERE
    description: "SAP Datasphere / BDC tenant REST API base URL"

mcp:
  translationFile: mcp-translation/translation.json
